
// Backend fetches are planned to be created here.
// Potentially axios will be added in future.
export default class FetchUtils {
    // static async fetchAnything() {
    //     return await fetch("...").then(res => res.json());
    // }
}